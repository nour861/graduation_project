import 'package:flutter/material.dart';

class Chate extends StatelessWidget {
  Map<String,dynamic>json;
  int type;
  Chate(this.json,this.type);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
